package com.cognitran.classes.topic4.figure;

/**
Utwórz klasę immutable o nazwie NumberWrapper, której jedyny konstruktor przyjmuje liczbę (double).
Klasa powinna mieć następujące metody:
    boolean isOdd() - true jeśli przekazany atrybut jest nieparzysty,
    boolean isEven() - true jeśli przekazany atrybut jest parzysty,
    NumberWrapper sqrt() - pierwiastek z przekazanego atrybutu w konstruktorze (Math.sqrt),
    NumberWrapper pow(NumberWrapper x) - atrybut podniesiony do potęgi x (Math.pow),
    NumberWrapper add(NumberWrapper x) - zwraca sumę atrybutu i x opakowaną w klasę NumberWrapper,
    NumberWrapper subtract(NumberWrapper x) - zwraca różnicę atrybutu i x opakowaną w klasę NumberWrapper.
 */
public class NumberWrapper
{
}
