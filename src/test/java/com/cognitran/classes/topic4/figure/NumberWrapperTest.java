package com.cognitran.classes.topic4.figure;

import org.junit.Test;

public class NumberWrapperTest
{
    @Test
    public void test()
    {
        //NumberWrapper wrapper = new NumberWrapper(4.0);
        //assertFalse(wrapper.isOdd());
        //assertTrue(wrapper.isEven());
    }
}
